{
  "name": "h2insurance-solutions",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "npm run build && wrangler dev",
    "deploy": "npm run build && wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/vite-plugin": "^1.38.0",
    "vite": "^6.3.0",
    "wrangler": "^4.94.0"
  },
  "dependencies": {
    "mimetext": "^3.0.28"
  }
}
